# SPDX-FileCopyrightText: 2026 geisserml <geisserml@gmail.com>
# SPDX-License-Identifier: Apache-2.0 OR BSD-3-Clause

name: Update Docs (GH Pages)

on:
  workflow_call:
    inputs: &reusable_inputs
      publish:
        type: boolean
        default: false
  workflow_dispatch:
    inputs: *reusable_inputs

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

defaults:
  run:
    shell: bash
permissions: {}

jobs:
  
  build:
    name: Build docs
    permissions:
      contents: read  # recommended by checkout and setup-python actions
    
    runs-on: ubuntu-slim
    steps:
      
      - name: Check out pypdfium2 (deep)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0  # for git describe in conf.py
          persist-credentials: false
      
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.14'
      
      - name: Create venv
        uses: ./.github/actions/venv_step
      
      - name: Install pip packages
        run: pip install --uploaded-prior-to "${PIP_UPLOADED_PRIOR_TO:?}" -U --group docs
      
      - name: Install pypdfium2
        run: python3 -m pip install -v .
      
      - name: Build documentation
        run: python3 -m sphinx -b html docs/source docs/build/html
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: 'docs/build/html'
  
  publish:
    name: Publish to GH Pages
    needs: build
    if: ${{ inputs.publish }}
    
    permissions:
      pages: write     # Deploy to GH pages
      id-token: write  # Deploy to GH pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    runs-on: ubuntu-slim
    steps:
      - name: Deploy to GH Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
