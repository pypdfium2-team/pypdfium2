# SPDX-FileCopyrightText: 2026 geisserml <geisserml@gmail.com>
# SPDX-License-Identifier: Apache-2.0 OR BSD-3-Clause

name: Trigger conda_raw release
on:
  schedule:
    # pdfium-binaries triggers conda on the first Monday of month at 4 o'clock UTC, so we'll want to rebuild after that, but before the next main release where we want to use the package
    - cron: '0 4 8 * *'  # monthly, 8th day
  workflow_dispatch:

permissions: {}

jobs:
  # NOTE recently changed to reusable workflow call, let's see if this works. If it does not, we can revert back to benc-uk/workflow-dispatch.
  conda_raw_release:
    uses: ./.github/workflows/conda.yaml
    permissions:
      contents: read  # needed by called workflow
    secrets:
      anaconda_api_token: ${{ secrets.ANACONDA_API_TOKEN }}
    with:
      package: raw
      new_only: true
      test: true
      publish: true
      actually_publish: true
