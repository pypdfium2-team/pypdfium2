# SPDX-FileCopyrightText: 2026 geisserml <geisserml@gmail.com>
# SPDX-License-Identifier: Apache-2.0 OR BSD-3-Clause

name: Test one
on:
  workflow_call:
    inputs:
      runner_os:
        type: string
        required: true
      artifact_id:
        type: string
        required: true
      py_vers:
        type: string
        default: '3.14'
      ref:
        type: string
        default: ''

defaults:
  run:
    shell: bash
permissions: {}

jobs:
  test:
    name: Test on ${{ inputs.runner_os }}
    permissions:
      contents: read  # recommended by checkout and setup-python actions
    
    runs-on: ${{ inputs.runner_os }}
    steps:
      
      - name: Check out pypdfium2 (shallow)
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          persist-credentials: false
          ref: ${{ inputs.ref || github.ref_name }}
      
      - name: Prepare
        uses: ./.github/actions/prepare_step
        with:
          py_vers: ${{ inputs.py_vers }}
      
      - name: Download artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          artifact-ids: ${{ inputs.artifact_id }}
          path: dist/
      
      - name: Test with Py ${{ inputs.py_vers }}
        run: python3 utils/multitest.py -w dist/*.whl --py-vers $PY_VERS --venv
        env:
          PY_VERS: ${{ inputs.py_vers }}
