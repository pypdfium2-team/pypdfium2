diff --git a/BUILD.gn b/BUILD.gn
index 25831ae..6ac445d 100644
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -94,11 +94,14 @@ source_set("libpng_sources") {
       "mips/filter_msa_intrinsics.c",
       "mips/mips_init.c",
     ]
-  } else if (current_cpu == "loong64" && loongarch64_use_lsx) {
+  } else if (current_cpu == "loong64" && true) {  # loongarch64_use_lsx
+    # loongarch64_use_lsx is defined in build/config/loongarch64.gni (default false).
+    # Patch to true here for simplicity so we don't need to patch across multiple git repos.
     sources += [
       "loongarch/filter_lsx_intrinsics.c",
       "loongarch/loongarch_lsx_init.c",
     ]
+    defines += ["PNG_LOONGARCH_LSX_OPT=1"]
   }
 
   configs -= [ "//build/config/compiler:chromium_code" ]
